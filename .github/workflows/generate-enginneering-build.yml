name: Generate Engineering Build

on:
  push:
    branches: 
      - 'main'
    workflow_dispatch:

jobs:
  call-workflow:
    name: Generate EngineeringBuild
    permissions:
      actions: read
      contents: read
    uses: ./.github/workflows/android-ci-workflow.yml
    with:
      scout_checkout_ref: main
      scout_build_name: EngineeringBuild
